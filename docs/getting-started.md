# Getting Started

```bash
cp .env.example .env.development
make install
make docker-up
```

Then open http://localhost:8000/docs. See the root `README.md` for the
Acumatica-specific setup steps (publishing GIs, field-name mapping).
